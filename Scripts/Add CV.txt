$noop(This adds the CV credit to releases that have credited the voice actor and the character but don't make use of the CV. standard)
$set(artist,$replace(%artist%,\(,\(CV.))
$set(albumartist,$replace(%albumartist%,\(,\(CV.))
$set(artistsort,$replace(%artistsort%,\(,\(CV.))
$set(albumartistsort,$replace(%albumartistsort%,\(,\(CV.))
$set(artist,$replace(%artist%, \(CV.,\(CV.))
$set(albumartist,$replace(%albumartist%, \(CV.,\(CV.))
$set(artistsort,$replace(%artistsort%, \(CV.,\(CV.))
$set(albumartistsort,$replace(%albumartistsort%, \(CV.,\(CV.))
